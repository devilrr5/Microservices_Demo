spring:
  application:
    name: Welcome-Service

  boot:
    admin:
      client:
        url: http://localhost:1111
##this admin server url




   
#eureka:
#  client: 
#    service-url:
#      defaultZone: https://localhost:8761/eureka
# if eureka server port no is 8761 no need to config else config like this

management:
  endpoints:
    web:
      exposure:
        include: '*'
        