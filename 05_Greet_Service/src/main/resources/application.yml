spring:
  application:
    name: Greet-Service

  boot:
    admin:
      client:
        url: http://localhost:1111
##this admin server url || and this is use for add in admin server


server:
  port: 9091

   
#eureka:
#  client:
#    service-url:
#      defaultZone: https://localhost:8761/eureka
# if eureka server port no is 8761 no need to config else config like this

management:
  endpoints:
    web:
      exposure:
        include: '*'
        